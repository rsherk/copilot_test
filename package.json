{
  "name": "markdown-editor",
  "version": "1.0.0",
  "description": "Single-page markdown editor with live preview and persistence",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "dev": "npx http-server dist -p 8080",
    "serve": "npx http-server dist -p 8080 -o",
    "clean": "node scripts/clean.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "marked": "^12.0.0",
    "dompurify": "^3.0.6"
  }
}
